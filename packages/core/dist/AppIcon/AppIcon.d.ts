import { AppIconProps } from "./AppIconProps";
/**
 * Icon Component used to display the icon of an application
 */
declare const AppIcon: {
    ({ title, size, color, ...props }: AppIconProps): JSX.Element;
    displayName: string;
};
export default AppIcon;
