/*
import { Extension, mergeAttributes } from '@tiptap/core';

declare module '@tiptap/core' {
  interface Commands<ReturnType> {
    linker: {...};
  }
}

const Linker = Extension.create({
  name: 'linker',
...

})
*/
